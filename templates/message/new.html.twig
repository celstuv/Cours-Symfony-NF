{% extends "base.html.twig" %}

{% block body %}
  {{ form_start(formMessage, {'attr' : { 'class' : 'text-center border border-light p-5'}})}}  
   

        <p class="h4 mb-4">Votre message</p>

  
        <div class="md-form">
          {{ form_widget(formMessage.sender)}}
          {{ form_label(formMessage.sender) }}
          
        </div>
        {{ form_errors(formMessage.sender) }}
        {# Affiche l'input associé à sender #}
        {# Ce qui signifie ici : <input type="text" id="defaultLoginFormEmail" class="form-control mb-4" placeholder="Votre nom"> #}

 
        <div class="md-form">
          {{ form_widget(formMessage.receiver)}}
          {{ form_label(formMessage.receiver) }}
        </div>
        <div class="md-form">
          {{ form_widget(formMessage.body)}}
          {{ form_label(formMessage.body) }}
        </div>
    
   
   {{ form_end(formMessage)}}  
{% endblock %}
